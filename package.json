{
  "name": "multiagent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Interactive labs for multi-agent shared-resource games with known solutions",
  "license": "MIT",
  "scripts": {
    "dev": "vite --port 4173",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --port 4173",
    "test": "vitest run",
    "check": "npm run test && npm run build"
  },
  "devDependencies": {
    "happy-dom": "^20.11.1",
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vitest": "^2.1.4"
  }
}
